{
  "name": "Rails OpenID project",
  "description": "Rails project connecting to OpenID provider",
  "repository": "https://github.com/Deauthorized/rails_openidconnect_starter_app",
  "keywords": [
    "Ruby",
    "Rails",
    "OpenID Connect Client"
  ],
  "env": {
    "OPENID_CLIENT_ID": {
      "description": "Client ID registered on OpenID provider"
    },
    "OPENID_CLIENT_SECRET": {
      "description": "Client secret provided from OpenID provider"
    },
    "OPENID_PROVIDER_URL": {
      "description": "OpenID provider URL"
    },
    "OPENID_PORT": {
      "description": "OpenID provider port number"
    },
    "OPENID_ISSUER": {
      "description": "OpenID issuer URL"
    }
  },
  "image": "heroku/ruby"
}